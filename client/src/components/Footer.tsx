export default function Footer() {
  return (
    <footer className="fixed bottom-0 left-0 right-0 py-4 text-center bg-background/80 backdrop-blur-sm border-t" data-testid="footer">
      <p className="text-xs italic text-muted-foreground">
        Made to remind us that corruption begins with choices.
      </p>
    </footer>
  );
}
